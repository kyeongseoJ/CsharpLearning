using UnityEngine;

public class KyeongSeoJeon_Chapter4 : MonoBehaviour
{
    //1. i++와 ++i의 차이점은 무엇인가요?
    //    i++ 은 계산 전의 값이 나오고 계산이 나중에 적용된다. 선출력 후연산
    //    ++i 는 계산이 먼저 적용된 후 값이 나온다. 선연산 후출력

    //2. 각 출력문의 값을 적으세요.
    //int i = 1;
    //    Debug.Log(i = i + 1); 답 : 2
    //    Debug.Log(i++); 답 : 2
    //    Debug.Log(++i); 답 : 4
    //    Debug.Log(i += 1); 답 : 5

    //3. 다음 코드에서 b는 어떤 값을 가질까요?
    //int a = 10;
    //string b = a == 0 ? "가나다" : "ABC";
    //답 : "ABC"
}
